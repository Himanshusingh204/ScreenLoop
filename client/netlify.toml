# Netlify deployment config for Screenloop (SPA)

[build]
  base = "client/"
  command = "npm run build"
  publish = "dist"

# SPA redirect: send all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
