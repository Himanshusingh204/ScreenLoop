{
  "name": "screenloop-server",
  "version": "1.0.0",
  "description": "Signaling server for Screenloop",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "format": "prettier --write src/"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "pino": "^10.3.1",
    "socket.io": "^4.7.4"
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "nodemon": "^3.1.0",
    "prettier": "^3.9.6",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
