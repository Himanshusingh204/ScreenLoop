services:
  - type: web
    name: screenloop-server
    env: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: PORT
        value: 10000
      - key: ALLOWED_ORIGIN
        value: https://screen-loop.vercel.app
